export default function Hover() {

    const handleMouseOver = (data) => data

    const handleMouseOut = () => {

    }

    return {
        handleMouseOver: handleMouseOver,
        isHovered: handleMouseOut
    }
}